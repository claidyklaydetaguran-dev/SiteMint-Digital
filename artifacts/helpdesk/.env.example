# Voice-platform navigation/routes are hidden unless this is "true".
# Production-safe default is false. Never put secrets or provider
# credentials in VITE_ variables — this is a boolean capability switch only.
VITE_VOICE_PLATFORM_ENABLED=false
