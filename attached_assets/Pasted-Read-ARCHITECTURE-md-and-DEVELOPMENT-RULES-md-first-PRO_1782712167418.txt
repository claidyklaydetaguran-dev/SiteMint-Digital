Read ARCHITECTURE.md and DEVELOPMENT_RULES.md first.

PRODUCT QA PASS — Communications Center Freeze

MODULE:
Communications Center only.

GOAL:
Verify the new Communications Center works end-to-end, fix only bugs/regressions, then mark it STABLE.

DO:

* Audit /admin/crm/communications.
* Test all Communications Center tabs.
* Test backward-compatible routes.
* Fix only bugs, broken clicks, bad empty/loading/error states, React warnings, TypeScript issues, or nav issues.
* Update ARCHITECTURE.md to mark Communications Center STABLE after tests pass.

DO NOT:

* Add new features.
* Change database schema.
* Touch Twilio routes.
* Touch Campaign scheduler/sending.
* Refactor unrelated modules.
* Start Sales OS yet.

TEST:

1. Main route

* /admin/crm/communications loads.
* COMMUNICATIONS sidebar group auto-expands.
* Active state highlights Communications Center.

2. Conversations tab

* Thread list loads.
* 30-second polling does not duplicate messages.
* SMS composer opens/sends or fails safely if Twilio env is missing.
* Lead panel populates.
* Workspace link routes to /admin/crm/leads/:id.
* Empty state works if no threads.

3. Email Activity tab

* Table loads.
* Search filters by contact, subject, campaign/source if supported.
* Test/live badge displays.
* Workspace links work.
* Empty state works if no sent emails.

4. Templates tab

* Template list loads.
* Create template works.
* Edit template works.
* Delete template works.
* Empty state works.

5. Backward compatibility

* /admin/crm/inbox still works.
* /admin/crm/email-templates still works.

6. Navigation/topbar

* Global search still works.
* New Contact modal still works.
* Email/SMS/Phone quick actions still work.
* Profile/logout still works.

7. Quality

* Browser console has no React key warnings.
* No hook violations.
* TypeScript clean:
  pnpm --filter @workspace/api-server run typecheck
  pnpm --filter @workspace/web-agency run typecheck

FINAL REPORT:
Return:

1. Files inspected
2. Issues found
3. Files changed
4. Fixes made
5. QA results by tab
6. Backward compatibility results
7. Typecheck results
8. Confirmation no schema/Twilio/Campaign scheduler changes
9. ARCHITECTURE.md updated to STABLE
10. Remaining gaps
11. Recommendation to proceed to Product 2 — Sales OS
